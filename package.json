{
  "name": "ng-helper-cli",
  "bin": {
    "ng-helper-cli-dev": "./dist/bin.js",
    "tng-helper-cli": "./build/bin.js"
  },
  "version": "0.0.0",
  "main": "bin.js",
  "scripts": {
    "dev": "tsc -w",
    "build": "esbuild ./src/bin.ts --bundle --platform=node --target=node22.0 --outfile=./build/bin.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Helper to setup angular project to my liking",
  "devDependencies": {
    "@types/node": "^22.12.0",
    "esbuild": "^0.25.0",
    "prettier": "3.5.3",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "commander": "^13.1.0"
  }
}
